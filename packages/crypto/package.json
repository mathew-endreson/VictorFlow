{
  "name": "@victorflow/crypto",
  "version": "0.1.0",
  "private": true,
  "description": "Node-only crypto primitives: tracking HMAC, constant-time compare, Ed25519 licence signing/verification",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsup",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsup": "^8.5.0",
    "typescript": "~5.9.3",
    "vitest": "^3.2.0"
  }
}
