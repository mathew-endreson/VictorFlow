{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core defaults plus opening http(s)/mailto/tel links in the system browser. No filesystem or shell access.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
